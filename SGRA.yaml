_include:
  - (pfb)stimela_cabs.yml

opts:
  log:
    dir: logs
    nest: 2
  backend:
    select: singularity

sgraimage:
  info: SGRA imaging recipe

  assign_based_on:
    obs:
      'sgra':
        ms: [/home/bester/projects/GC/msdir/1590089458_sdp_l0_1024ch_SGRA.ms]
      'gcx17':
        ms: [/home/bester/projects/GC/msdir/1590089458_sdp_l0_1024ch_GCX17.ms]
      'gcx30':
        ms: [/home/bester/projects/GC/msdir/1590089458_sdp_l0_1024ch_GCX30.ms]

  inputs:
    basedir:
      default: '/home/bester/projects/GC/output'
      info:
        Base directory in which to place output data products.
    obs:
      default: sgra
      choices: ['sgra','gcx17','gcx30']
    freq-range:
      default: '570230468.75:1009042968.75'  # clip fully flagged parts
    output-filename:
      default: '{recipe.basedir}/{obs}/test0'
      aliases: ['*.output-filename']
      info:
        Observation specific name to place pfb outputs
    fits-output-folder:
      default: '{recipe.basedir}/{obs}/fits'
      aliases: ['*.fits-output-folder']
      info:
        Directory to place fits files
    log-directory:
      default: '{recipe.basedir}/{obs}/logs'
      aliases: ['*.log-directory']
      info:
        Directory in which to place logs
    channels-per-image:
      default: 69  # results in 12 bands
      info:
        Number of channels per image.
        Determines number of output bands.
    integrations-per-image:
      default: -1
      info:
        Number of integrations per image.
    robustness:
      dtype: float
      default: -0.5
      info:
        Briggs robustness level.
        Less than -2 corresponds to uniform, larger than 2 to natural.
    field-of-view:
      dtype: float
      default: 3.5
    super-resolution-factor:
      dtype: float
      default: 2.5
      info:
        How much to oversample Nyquist by at the highest frequency
    chan-average:
      dtype: int
      default: 1
      info:
        How many channels to average together
    bda-decorr:
      dtype: float
      default: 0.98
      info:
        Amount of amplitude loss that can be tolerated at edge of field
    niter:
      dtype: int
      default: 15
      info:
        Number of major cycles
    l1-reweight-from:
      dtype: int
      default: 7
    rmsfactor:
      dtype: float
      default: 1.0
      info:
        Multiple of the rms to threshold by
    overwrite:
      default: true
      aliases: ['*.overwrite']

  steps:
    init:
      cab: pfb.init
      info: 'Initialise imaging data products'
      params:
        ms: =recipe.ms
        output-filename: '{recipe.basedir}/{recipe.obs}_{recipe.band}_bda{recipe.bda-decorr}_fov{recipe.field-of-view}'
        nthreads: 1
        nworkers: 64
        sigma-column: SIGMA_SPECTRUM
        channels-per-image: =recipe.channels-per-image
        integrations-per-image: =recipe.integrations-per-image
        chan-average: =recipe.chan-average
        max-field-of-view: =recipe.field-of-view
        bda-decorr: =recipe.bda-decorr
        freq-range: =recipe.freq-range
        overwrite: =recipe.overwrite
      tags: [init]

    grid:
      cab: pfb.grid
      info: 'Setup grid for imaging'
      params:
        output-filename: '{recipe.basedir}/{recipe.band}_bda{recipe.bda-decorr}_fov{recipe.field-of-view}'
        super-resolution-factor: =recipe.super-resolution-factor
        field-of-view: =recipe.field-of-view
        robustness: =recipe.robustness
        nthreads: 7
        nworkers: 10
      tags: [image]

    sara:
      cab: pfb.sara
      info: Cleaning step
      params:
        output-filename: '{recipe.basedir}/{recipe.band}_bda{recipe.bda-decorr}_fov{recipe.field-of-view}'
        niter: =recipe.niter
        bases: 'self,db1,db2,db3,db4'
        nlevels: 2
        l1-reweight-from: =recipe.l1-reweight-from
        pd-tol: [1e-4]
        pd-maxit: 450
        pd-verbose: 2
        pd-report-freq: 50
        tol: 1e-3
        gamma: 0.9
        rmsfactor: =recipe.rmsfactor
        epsfactor: 1.0
        positivity: 1
        nthreads: 64
      tags: [image]

    restore:
      cab: pfb.restore
      info: Produce restored image.
      params:
        output-filename: '{recipe.basedir}/{recipe.band}_bda{recipe.bda-decorr}_fov{recipe.field-of-view}'
        nworkers: 10
        nthreads: 5
        outputs: mMrRiI
      tags: [final]
